services:
  postgres:
    image: postgres:16
    container_name: postgres
    environments:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: ecommerce
    ports:
      - "5433:5432"